export interface Theater {
  id?: string;
  name: string;
  address: string;
  city: string;
  state: string;
  state_long: string;
  lat: number;
  lng: number;
  description?: string;
  website?: string;
}
